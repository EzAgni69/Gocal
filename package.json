{
    "name": "gocal-monorepo",
    "private": true,
    "packageManager": "npm@11.6.0",
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\"",
        "test": "turbo run test"
    },
    "devDependencies": {
        "@types/jest": "^30.0.0",
        "jest": "^30.3.0",
        "prettier": "latest",
        "ts-jest": "^29.4.6",
        "ts-node": "^10.9.2",
        "turbo": "latest"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "dependencies": {
        "firebase": "^12.9.0"
    }
}
